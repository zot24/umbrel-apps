manifestVersion: 1.1
id: zot24-proton-bridge
name: Proton Bridge
icon: https://raw.githubusercontent.com/zot24/umbrel-apps-gallery/main/zot24-proton-bridge/icon.svg
category: networking
version: "0.1.4"
tagline: Proton Mail IMAP/SMTP bridge for sibling apps on your Umbrel
description: >-
  Runs Proton Mail Bridge as a background IMAP/SMTP gateway on your Umbrel so
  other apps on the box (Hermes + Himalaya, scripts) can read and send mail
  without putting Proton keys inside those apps.


  Proton does not offer direct IMAP. Bridge decrypts locally and speaks IMAP
  on port 143 and SMTP on port 25 inside this container. Those ports are not
  published to the host or the internet. Sibling apps reach them over the
  Umbrel Docker network.


  The app tile is an operator console (ttyd) behind two gates: your Umbrel
  login, then HTTP basic auth. Username is `bridge`. Password is the one
  Umbrel shows on this app's tile (same pattern as nworth). First run: open
  the tile, type `login-cli`, sign in with Proton + 2FA, then `info` to copy
  the mailbox password into 1Password. Mail stays up after you close the tab.


  Requirements:

  - A paid Proton plan that includes Bridge (Mail Plus or higher). Free
    accounts are refused.

  - You log in once in this app's console. Do not paste Proton passwords into
    chat.


  Security notes:

  - The console is Umbrel-login then ttyd basic auth (tile password). Do not
    publish port 7681 or tunnel this app to the public internet.

  - IMAP/SMTP are internal. Do not add host port mappings.

  - This wraps the unofficial shenxn/protonmail-bridge image (Proton Bridge
    is GPL-3.0). Keys live on this app's data volume, not in Hermes.
releaseNotes: >-
  0.1.4: console password on the app tile (Umbrel login + ttyd basic auth).
  Username `bridge`, password from this tile. 0.1.3: gpg+pass on start.
developer: zot24
website: https://github.com/zot24/umbrel-apps
dependencies: []
repo: https://github.com/zot24/umbrel-apps
support: https://github.com/zot24/umbrel-apps/issues
port: 7685
gallery: []
path: ""
defaultUsername: "bridge"
defaultPassword: ""
deterministicPassword: true
torOnly: false
submitter: zot24
submission: ""
