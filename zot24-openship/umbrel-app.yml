manifestVersion: 1.1
id: zot24-openship
name: Openship
icon: https://raw.githubusercontent.com/zot24/umbrel-apps-gallery/main/zot24-openship/icon.svg
category: developer
version: "0.6.9.1"
tagline: Self-hosted deploy control plane (dashboard + API)
description: >-
  Openship is a self-hosted deployment platform. This Umbrel app runs the
  control plane only: Postgres, Redis, API, and dashboard, behind the Umbrel
  tile.


  It does NOT install Openship's OpenResty edge on ports 80/443 — Umbrel
  already owns those. It does NOT mount the host Docker socket, so it will
  not build or run apps on this Umbrel. Use the dashboard to manage remote
  servers over SSH, or treat this as an always-on Openship UI.


  First browser to the tile registers the admin (`OPENSHIP_AUTH_MODE=local`).
  After that, signup is invite-only (Openship's own auth; Umbrel proxy login
  is off). Zero-auth / loopback-only mode is disabled on purpose — the Umbrel
  tile is not localhost.


  If login fails with 403 ORIGIN_REJECTED, you opened the tile on a host
  that is not http://umbrel.local:3001. Add OPENSHIP_PUBLIC_URL to
  docker-compose.yml (data/.env is root-owned) and restart the app.

  Upstream: https://github.com/oblien/openship (images ghcr.io/oblien,
  v0.6.9, sha256-pinned).
releaseNotes: >-
  0.6.9.1: OPENSHIP_AUTH_MODE=local so the Umbrel tile can create an admin.
  Zero-auth only works on localhost; the tile is not that.
developer: zot24
website: https://github.com/oblien/openship
dependencies: []
repo: https://github.com/zot24/umbrel-apps
support: https://github.com/zot24/umbrel-apps/issues
port: 3001
gallery: []
path: ""
defaultUsername: ""
defaultPassword: ""
deterministicPassword: false
torOnly: false
submitter: zot24
submission: ""
